name: Test validator site

on:
  pull_request:
    branches: [ master ]
  push:
    branches: [ feature/test-data-model-validator-site ]

jobs:
  test-validator-site:
    uses: openactive/data-model-validator-site/.github/workflows/test-and-deploy.yml@feature/reusable-validator-site
    with:
      data-models_ref: {{ github.ref }}
