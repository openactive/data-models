/*!
 * data-models
 * MIT Licensed
 */

'use strict';

exports = module.exports = createExports();

function createExports() {

    let root = {
        loadModel: require('./loadModel')
    };

    return root;
}
